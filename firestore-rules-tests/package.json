{
  "name": "yo-te-llevo-firestore-rules-tests",
  "description": "Tests automatizados del archivo firestore.rules (spec Módulo 9). Usa @firebase/rules-unit-testing con el emulador de Firestore in-process.",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --runInBand --watch"
  },
  "engines": {
    "node": "20"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.8",
    "firebase": "^10.13.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
